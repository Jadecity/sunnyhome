from django.shortcuts import render, HttpResponse

# Create your views here.
def homepage(request, name):
    return HttpResponse( "homepage got!^-^ name : %s" %name )

def page404(request):
    return HttpsResponse("sorry....")
